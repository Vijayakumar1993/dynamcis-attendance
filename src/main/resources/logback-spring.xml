<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Write logs to logs/app.log -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>

        <!-- Rolling policy: rotate, zip, keep only 5 zips -->
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- Log file name pattern for zip archives -->
            <fileNamePattern>logs/archive/app-%d{yyyy-MM-dd}.%i.zip</fileNamePattern>

            <!-- Max size for each log file before rolling -->
            <maxFileSize>10MB</maxFileSize>

            <!-- Keep only 5 zip files (clean up older automatically) -->
            <maxHistory>5</maxHistory>

            <!-- Total size cap to prevent disk filling (optional) -->
            <totalSizeCap>60MB</totalSizeCap>
        </rollingPolicy>

        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Console logging (optional) -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss} %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="FILE"/>
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>